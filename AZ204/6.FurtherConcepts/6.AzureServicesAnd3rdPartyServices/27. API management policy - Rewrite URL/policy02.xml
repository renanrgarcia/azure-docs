<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
    <!-- Throttle, authorize, validate, cache, or transform the requests -->
    <inbound>
        <base />
        <set-variable name="id" value="@(context.Request.Url.Query.GetValueOrDefault(&quot;id&quot;))" />
        <choose>
            <when condition="@(string.IsNullOrEmpty((string)context.Variables.GetValueOrDefault(&quot;id&quot;)))">
                <return-response>
                    <set-status code="400" reason="Bad Request" />
                    <set-body>{"error":"Missing required query parameter: id"}</set-body>
                </return-response>
            </when>
        </choose>
        <rewrite-uri template="@(&quot;/api/orders/&quot; + (string)context.Variables.GetValueOrDefault(&quot;id&quot;))" />
    </inbound>
    <!-- Control if and how the requests are forwarded to services  -->
    <backend>
        <base />
    </backend>
    <!-- Customize the responses -->
    <outbound>
        <base />
    </outbound>
    <!-- Handle exceptions and customize error responses  -->
    <on-error>
        <base />
    </on-error>
</policies>